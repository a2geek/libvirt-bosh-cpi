#!/bin/bash

pkgs_dir=${BOSH_PACKAGES_DIR:-/var/vcap/packages}
jobs_dir=${BOSH_JOBS_DIR:-/var/vcap/jobs}

exec $pkgs_dir/libvirt_cpi/bin/cpi \
     -logLevel <%= p("libvirt_cpi.log_level") %> \
     -configPath $jobs_dir/libvirt_cpi/config/cpi.json
